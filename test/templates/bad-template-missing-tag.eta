/*-------------------------------------------------------------------------------------------
 * extended template with missing ending tag
 * -----------------------------------------------------------------------------------------*/

<% names.forEach((name, i) => {
%> <%= i === names.length - 1 ? titleCase(name) : titleCase(name) + '\n' %><%});
